import { Body } from '../components'

/**
 * The Home page with all the components. Same as the Display page but with a button to enter into the admin options
 * Route: /
 */
export default function Home() {

    return (
       <Body></Body>
    )
}
