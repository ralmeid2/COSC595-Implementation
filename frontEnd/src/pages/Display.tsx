import { Body } from '../components'

/**
 * Main display page for showing all the components. Same as the Home page but no button to enter into the admin options
 * Route: /display
 */
export default function Display(){
    return(

        <Body />
    )
}
